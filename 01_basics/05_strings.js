const name = "manoj"
const lname = "maddhehsiya"

// console.log(name + lname) 

// string interpolation’s 
console.log(`Hello My name is ${name} and my lname is ${lname}`);

const gameName = new String('manoj')

console.log(gameName[0])

// method length, .toUpeerCase, .charAt, indexOf, subString(- nhi de sakte hai), slice ke andar aap negative value bhi use kar sakte hai,
// trim(starting and ending space trim kar deta hai )
// replace()
// split()